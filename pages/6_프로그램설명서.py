import streamlit as st
from core.auth import ensure_login
from core.bootstrap import initialize_app

initialize_app()
ensure_login()
st.title('프로그램 설명서')
st.markdown(
    """
### 현재 수집 범위
- **채소팜**은 네이버 커머스 API로 가격을 가져옵니다.
- **경쟁사**는 `경쟁사 상품 설정`에 등록한 네이버 상품 상세 URL을 직접 확인합니다.
- 채소팜 API는 `discountedPrice`가 있으면 할인가를, 없으면 `salePrice`를 사용합니다.

### 경쟁사 상품 설정
- 상품별·경쟁사별 `검색여부` 체크박스와 상품 URL을 관리합니다.
- 기본값은 모두 체크 상태입니다. URL이 비어 있으면 실제 요청은 하지 않습니다.
- 검색규칙에서 새 상품을 추가하면 이 페이지에도 자동으로 행이 추가됩니다.
- 요청 사이 2~5초 간격을 두고, 실패 시 1회만 재시도합니다. CAPTCHA/접근 제한은 우회하지 않고 해당 가격을 빈칸 처리합니다.

### 검색 규칙
- 채소팜 검색어와 네이버 상품명을 비교해 상품을 찾습니다.
- 용량·수량 숫자가 다른 상품은 자동 매칭에서 제외합니다.

### 상품 ID 규칙
- 채널 상품번호 또는 원상품번호가 있으면 검색어보다 우선합니다.
- 상품이 잘못 매칭되면 해당 번호를 입력하는 방식이 가장 정확합니다.

### 가격 규칙
- `mul`: 곱하기, `add`: 더하기, `sub`: 빼기, `set`: 고정값, `rate`: 비율 적용입니다.
- 경쟁사 상품의 판매 단위가 다르면 경쟁사에도 동일하게 환산 규칙을 적용할 수 있습니다.

### 자동 실행
- 웹에서 수동 실행하면 선택된 경쟁사 URL까지 즉시 확인하고 결과를 저장합니다.
- 매일 08:30 Cron 실행도 같은 설정을 사용하며 결과를 텔레그램으로 발송합니다.
"""
)
